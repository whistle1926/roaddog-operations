{
  "name": "roaddog-operations",
  "version": "0.1.0",
  "description": "RoadDog Courier operations system: dispatch admin, customer portal and driver app",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html styles.css src dist/",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
